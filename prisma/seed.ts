/**
 * Geen automatische seed meer — gebruikers uploaden hun eigen SRA-checklist
 * via de UI. Het seed-script blijft bestaan zodat `db:reset` werkt.
 */
console.log('ℹ Geen seed-data; gebruikers uploaden checklists via de UI.');